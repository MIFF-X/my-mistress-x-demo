# Payments Feature

This path is reserved for feature-level payments modules.

Current plugin implementation is canonical under:
- `frontend/plugins/site/monetization/payments`
- `frontend/plugins/site/monetization/tip-jar`

Feature migration target remains:
- `frontend/features/payments`

Do not add new Site monetization source modules here. Keep this folder for feature-level payment surfaces only; Site plugin source belongs under the canonical plugin paths above.
