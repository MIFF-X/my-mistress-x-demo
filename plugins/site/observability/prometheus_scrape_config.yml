# prometheus_scrape_config.yml
scrape_configs:
  - job_name: 'mistress_x_metrics'
    metrics_path: /metrics
    static_configs:
      - targets: ['HOST:9100']
    # If metrics endpoint is behind basic auth or TLS, configure accordingly
