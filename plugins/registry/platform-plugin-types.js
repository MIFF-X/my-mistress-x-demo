export const PLATFORM_PLUGIN_CATEGORIES = Object.freeze({
  MONEY: "money",
  CONTENT: "content",
  LIVE: "live",
  COMMUNICATION: "communication",
  MARKETPLACE: "marketplace",
  COLLECTIBLES: "collectibles",
  ROLODEX: "rolodex",
  BADGES: "badges",
  STYLING: "styling",
  SMM: "smm",
  ADMIN: "admin",
  COMPLIANCE: "compliance",
  ANALYTICS: "analytics",
});

export const PLATFORM_PLUGIN_STATUS = Object.freeze({
  IDEA: "idea",
  LOCKED: "locked",
  SCAFFOLDED: "scaffolded",
  FRONTEND_READY: "frontend_ready",
  BACKEND_NEEDED: "backend_needed",
  DATABASE_NEEDED: "database_needed",
  ADMIN_NEEDED: "admin_needed",
  INTEGRATION_NEEDED: "integration_needed",
  SAFETY_REVIEW: "safety_review",
  DONE: "done",
});

export const PLATFORM_PLUGIN_VISIBILITY = Object.freeze({
  PUBLIC: "public",
  MISTRESS: "mistress",
  SUB: "sub",
  HEADMISTRESS: "headmistress",
  SYSTEM: "system",
});

export function createPlatformPlugin({
  id,
  name,
  category,
  description,
  status = PLATFORM_PLUGIN_STATUS.IDEA,
  visibility = [PLATFORM_PLUGIN_VISIBILITY.MISTRESS],
  progress = 0,
  frontend = "not_started",
  backend = "not_started",
  database = "not_started",
  admin = "not_started",
  dependencies = [],
  routes = [],
  docs = [],
  safetyNotes = [],
  nextTasks = {
    frontend: [],
    backend: [],
    database: [],
    admin: [],
    safety: [],
  },
} = {}) {
  return {
    id,
    name,
    category,
    description,
    status,
    visibility,
    progress,
    frontend,
    backend,
    database,
    admin,
    dependencies,
    routes,
    docs,
    safetyNotes,
    nextTasks: {
      frontend: nextTasks.frontend || [],
      backend: nextTasks.backend || [],
      database: nextTasks.database || [],
      admin: nextTasks.admin || [],
      safety: nextTasks.safety || [],
    },
  };
}
