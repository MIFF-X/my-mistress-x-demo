# UI Components

This folder consolidates shared UI component primitives.

Use sub-files/directories here for reusable primitives such as:
- buttons
- cards
- forms

Previous placeholder folders (`ui/buttons`, `ui/cards`, `ui/forms`) were merged into this single components namespace.
