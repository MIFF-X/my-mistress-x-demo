# Dashboards Compatibility Styles

This folder is legacy compatibility for older imports that still point at
`frontend/styles/dashboards/*`.

New dashboard-style source should use:

```text
frontend/styles/dashboard/
```

Keep files here as thin bridges only while remaining imports move to the
canonical singular `dashboard` path.
